import { AnyJson, JsonMap } from '@salesforce/ts-types';
/**
 * Parse JSON `string` data.
 *
 * @param {string} data Data to parse.
 * @param {String} [jsonPath=unknown] The file path from which the JSON was loaded.
 * @param {boolean} [throwOnEmpty=true] If the data contents are empty.
 * @returns {AnyJson}
 * @throws {JsonParseError} If the data contents are empty or the data is invalid.
 */
export declare function parseJson(data: string, jsonPath?: string, throwOnEmpty?: boolean): AnyJson;
/**
 * Parse JSON `string` data, expecting the result to be a `JsonMap`.
 *
 * @param {string} data Data to parse.
 * @param {String} [jsonPath=unknown] The file path from which the JSON was loaded.
 * @param {boolean} [throwOnEmpty=true] If the data contents are empty.
 * @returns {JsonMap}
 * @throws {JsonParseError} If the data contents are empty or the data is invalid.
 * @throws {JsonDataFormatError} If the data contents are not a `JsonMap`.
 */
export declare function parseJsonMap(data: string, jsonPath?: string, throwOnEmpty?: boolean): JsonMap;
/**
 * Perform a deep clone of an object or array compatible with JSON stringification.
 * Object fields that are not compatible with stringification will be omitted.  Array
 * entries that are not compatible with stringification will be censored as `null`.
 *
 * @param {T} obj A JSON-compatible object or array to clone.
 * @returns {T}
 * @throws {JsonStringifyError} If the object contains circular references or causes
 *  other JSON stringification errors.
 */
export declare function cloneJson<T extends object>(obj: T): T;
/**
 * Finds all elements of type `T` with a given name in a `JsonMap`.  Not suitable for use
 * with object graphs containing circular references.  The specification of an appropriate
 * type `T` that will satisfy all matching element values is the responsibility of the caller.
 *
 * @param {JsonMap} json The `JsonMap` tree to search for elements of the given name.
 * @param {string} name The name of elements to search for.
 * @returns {T[]} An array of matching elements.
 */
export declare function getJsonValuesByName<T extends AnyJson>(json: JsonMap, name: string): T[];
