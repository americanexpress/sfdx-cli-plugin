/**
 * Units of time
 */
export declare enum TIME_UNIT {
    MINUTES = 0,
    MILLISECONDS = 1,
    SECONDS = 2
}
/**
 * Helper class to convert cli times between minutes, seconds, and milliseconds.
 */
export declare class Time {
    static readonly MILLI_IN_SECONDS: number;
    static readonly SECONDS_IN_MINUTE: number;
    private readonly quantity;
    private readonly unit;
    constructor(quantity: number, unit?: TIME_UNIT);
    readonly minutes: number;
    readonly seconds: number;
    readonly milliseconds: number;
}
